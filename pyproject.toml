[build-system]
requires = [
    "setuptools>=36.2.1", # environment markers
    "setuptools_scm",
    "wheel",
]

[tool.setuptools_scm]
write_to = "src/pwa_pages/version.py"

[tool.black]
line-length = 79
target-version = [
    'py36',
    'py37',
    'py38',
]
include = '\.pyi?$'
exclude = '''
/(
    .*\.egg-info
    | .*build
    | \.eggs
    | \.git
    | \.tox
    | \.venv
    | \.vscode
    | dist
)/
'''

[tool.isort]
line_length = 79
profile = "black"
src_paths = ["src"]
