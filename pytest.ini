[coverage:run]
branch = True
source = src

[pytest]
addopts =
    --color=yes
    --doctest-continue-on-failure
    --doctest-modules
    --durations=3
    --ignore=docs/conf.py
filterwarnings =
    error
    ignore:Exception ignored in.*:UserWarning
testpaths =
    src
    tests
